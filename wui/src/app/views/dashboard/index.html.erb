          <div id="dataTableWrapper">

          <%= render :partial => "/layouts/quick_stats", :locals => { :hpool => @default_pool, :vms => @vms } %>

          <div class="dataTable">

              <div class="inside">

                <div class="data-table-column">
                <p> Hardware Pools are the high level organizational pool </p>
                </div>

                <div class="data-table-column">
                <p> Network Maps provide Host and Storage level mapping </p>
                </div>

                <div class="data-table-column">
                <p> Host Collections give boundries for VM Migrations </p>
                </div>

              </div> <!-- end #data-table.inside -->

              <%= render :partial => "/layouts/hardware_pool_type_list", :locals => { :organizational_pools => @organizational_pools, :network_maps => @network_maps, :host_collections => @host_collections } %>

            </div> <!-- end #dataTable -->

            <div class="data-section">
              <div class="data-section-header"><strong>Available Hosts</strong></div>
              <div class="data-section-stats">Statistics Data</div>
              <div class="data-section-table">
                <div class="inside">
                  <%= render :partial => "/host/list", :locals => { :hosts => @available_hosts } %>
                </div>
              </div>
            </div>

            <div class="data-section">
              <div class="data-section-header"><strong>Available Storage</strong></div>
              <div class="data-section-stats">Statistics Data</div>
              <div class="data-section-table">
                <div class="inside">
                  <%= render :partial => "/storage/list", :locals => { :storage_volumes => @available_storage_volumes } %>
                </div>
              </div>
            </div>

          </div> <!-- end #dataTableWrapper -->

          <div id="tools">

          <h3>Actions</h3>
          <%if @is_admin -%>
          <div class="actions create">
<!--
              <%= link_to 'Add Storage', { :controller => 'storage', :action => 'new', :hardware_pool_id => @default_pool}, { :class => "" } %>
              <hr/>
-->
              <%= link_to 'Create New Pool', { :controller => "pool", :action => 'new', :superpool => @default_pool}, { :class => "" } %>
          </div>
          <% end -%>

          <div class="actions users">
            <div><%= link_to_if @can_delegate, 'User Permissions', { :controller => 'permission', :action => 'new', :hardware_pool_id => @default_pool }, { :class => "edit" } %></div>
            <table>
              <tr><td><%= pluralize @default_pool.permissions.admins.size, "Admin" %></td></tr>
              <tr><td><%= pluralize @default_pool.permissions.delegates.size, "Delegate" %></td></tr>
              <tr><td><%= pluralize @default_pool.permissions.monitors.size, "Monitor" %></td></tr>
            </table>
          </div>

          </div> <!-- end #tools -->

<%- content_for :title do -%>
<%= _("Dashboard") %>
<%- end -%>

