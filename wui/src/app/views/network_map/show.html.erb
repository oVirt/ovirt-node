          <div id="dataTableWrapper">

            <%= render :partial => "/layouts/quick_stats", :locals => { :hpool => @network_map, :vms => [] } %>

            <div class="dataTable">

            <%= render :partial => "/layouts/hardware_pool_type_list", :locals => { :host_collections => @network_map.host_collections } %>

            </div> <!-- end #dataTable -->

            <div class="data-section">
              <div class="data-section-header"><strong>Available Hosts</strong></div>
              <div class="data-section-stats">Statistics Data</div>
              <div class="data-section-table">
                <div class="inside">

                </div>
              </div>
            </div>

          </div> <!-- end #dataTableWrapper -->

          <div id="tools">

            <h3>Actions</h3>
            <%if @is_admin -%>
            <div class="actions create">
              <%= link_to 'Add Storage to Map', { :controller => 'storage', :action => 'new', :hardware_pool_id => @network_map }, { :class => "" } %>
              <%= link_to 'Add Host to Map', { :controller => 'host', :action => 'list', :attach_to_pool => @network_map }, { :class => "" } %>
              <hr/>
              <%= link_to 'Create Collection', { :controller => "collection", :action => 'new', :superpool_id => @network_map }, { :class => "" } %>
            </div>
            <div class="actions edit">
              <%= link_to 'Edit Map Properties', { :action => 'edit', :id => @network_map }, { :class => "edit" } %>
            </div>
            <div class="actions remove">
              <%= link_to "Delete Map", { :controller => 'network_map', :action => 'destroy', :id => @network_map }, { :method => :post, :confirm => 'Are you sure you want to delete - %s?' % @network_map.name, :class => "" } %>
            </div>
            <%- end -%>
            <div class="actions">
              <%= link_to "Back to #{@network_map.organizational_pool.name}", { :controller => 'pool', :action => 'show', :id => @network_map.organizational_pool }, { :class => "" } %>
            </div>

            <div class="actions users">
              <div><%= link_to_if @can_delegate, 'User Permissions', { :controller => 'permission', :action => 'new', :hardware_pool_id => @network_map }, { :class => "edit" } %></div>
              <table>
                <tr><td><%= pluralize @network_map.permissions.admins.size, "Admin" %></td></tr>
                <tr><td><%= pluralize @network_map.permissions.delegates.size, "Delegate" %></td></tr>
                <tr><td><%= pluralize @network_map.permissions.monitors.size, "Monitor" %></td></tr>
              </table>
            </div>

          </div>

<%- content_for :title do -%>
<%= @network_map.name %>
<%- end -%>
