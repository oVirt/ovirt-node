          <div id="dataTableWrapper">

          <div class="dataTable">
              <div class="inside">

                <div class="data-table-column">
                <% form_tag :action => 'create', :id => @collection do %>
                  <%= render :partial => 'form' %>
                  <%= submit_tag 'Create Host Collection' %>
                <% end %>
               </div>

                <div class="data-table-column">
                  <strong>Existing Collections</strong>
                  <% for collection in @collection.network_map.host_collections %>
                  <div><%= link_to collection.name, { :controller => "collection", :action => "show", :id => collection }, { } %></div>
                  <% end %>
                </div>

              </div> <!-- end #data-table.inside -->
            </div> <!-- end #dataTable -->

          </div> <!-- end #dataTableWrapper -->

          <div id="tools">

          <h3>Actions</h3>
            <div class="actions">
              <%= link_to "Back to #{@collection.network_map.name}", { :controller => 'network_map', :action => 'show', :id => @collection.network_map }, { :class => "" } %>
            </div>
          </div> <!-- end #tools -->

<%- content_for :title do -%>
<%= _("Create New Host Collection") %>
<%- end -%>

