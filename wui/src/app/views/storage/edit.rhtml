<div id="left">
<h2><%= _("Editing a Storage Volume") %></h2>

<% form_tag :action => 'update', :id => @storage_volume do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Edit' %>
<% end %>

</div>

<div id="right">
  <h2>Actions</h2>
  <ul class="actions">
    <li class="action"><%= link_to 'Show', { :action => 'show', :id => @storage_volume }, { :class => "show" } %></li>
    <li class="action"><%= link_to "Back to #{@storage_volume.hardware_resource_group.name}", { :controller => 'pool', :action => 'show', :id => @storage_volume.hardware_resource_group.id }, { :class => "list" } %></li>
  </ul>
</div>
